streamlit>=1.32.0
pandas>=2.0.0
pyyaml>=6.0
# pywin32 is required on Windows to connect to a live Aspen Plus Dynamics session.
# It is listed here for reference; install manually on Windows:
#   pip install pywin32
# pywin32>=306
